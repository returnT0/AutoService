<UserControl x:Class="Autoservis.Views.AutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Autoservis.Views" xmlns:model="clr-namespace:Autoservis.Model"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
            <StackPanel Orientation="Vertical" Margin="0,0,0,0">
            <ListView Name="lvZakaznik" Margin="20,20,0,300" BorderBrush="#000000" BorderThickness="3" HorizontalAlignment="Left" Width="760" HorizontalContentAlignment="Left">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name"  Width="100" DisplayMemberBinding="{Binding Path= Jmeno}" />
                        <GridViewColumn Header="Surname" Width="100" DisplayMemberBinding="{Binding Path= Prijmeni}" />
                        <GridViewColumn Header="Phone number" Width="100" DisplayMemberBinding="{Binding Path= Telefon}" />
                        <GridViewColumn Header="Email" Width="150" DisplayMemberBinding="{Binding Path= Email}" />
                        <GridViewColumn Header="Address" Width="190" DisplayMemberBinding="{Binding Path= Adresa}" />
                        <GridViewColumn Header="Note" Width="100" DisplayMemberBinding="{Binding Path= Poznamky}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        <StackPanel Orientation = "Horizontal">
            <ListView  Name="lvAuta" MouseDoubleClick="lvAuta_MouseDoubleClick" Margin="20,100,0,20" BorderBrush="#000000" BorderThickness="3" ItemsSource="{Binding Path=Auto}" HorizontalAlignment="Left" Width="600" HorizontalContentAlignment="Left" >
                 <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Brand" Width="100"  DisplayMemberBinding="{Binding Path= ZnackaVozu}" />
                        <GridViewColumn Header="Model" Width="100"  DisplayMemberBinding="{Binding Path= ModelVozu}" />
                        <GridViewColumn Header="SPZ" Width="100" DisplayMemberBinding="{Binding Path= Spz}" />
                        <GridViewColumn Header="VIN" Width="200" DisplayMemberBinding="{Binding Path= Vin}" />
                        <GridViewColumn Header="Color" Width="100" DisplayMemberBinding="{Binding Path= Barva}" />
                    </GridView>
                </ListView.View>
                </ListView>
            
            <StackPanel Orientation="Vertical" Margin="30,100,0,0">

                <Button Name="pridat" Content="Add" Foreground="White" Background="#000000" Height="30" HorizontalAlignment="Left" Margin="15,10,0,0" VerticalAlignment="Top" Width="100" Click="Button_Click"/>
                <Button Name="odebrat" Content="Delete" Foreground="White" Background="#000000" Height="30" HorizontalAlignment="Left" Margin="15,10,0,0" VerticalAlignment="Top" Width="100" Click="Button_Click_Odebrat"/>
                <Button Name="editovat" Content="Edit" Foreground="White" Background="#000000" Height="30" HorizontalAlignment="Left" Margin="15,10,0,0" VerticalAlignment="Top" Width="100" Click="Button_Click_Edit"/>
                <Button Name="return" Content="↩" Foreground="White" Background="#000000" Height="30" HorizontalAlignment="Left" Margin="15,10,0,0" VerticalAlignment="Top" Width="100" Click="konec_Click"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
